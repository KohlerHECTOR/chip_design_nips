transformer policies for POMDPs
more actions (wolpertinger)
off policy value based rl
better estimates of V*
todo tables (gnns, statesactions)
remove  caveats histogram remove limits 1 seed hp search
Now, given all the results, the limitations and caveats, smooth the text. Make the message centered around the ablation of graph representations used since the 2020 MLCAD papers increasingly without proper understanding and the potential 1-WL limits. Dont hesitate to hypothethize what could cause such penomena but use confitional in that case. Reference RL papers that were not rferenced.  @neurips_2026.bib  . Add two survey table one for RL on AIGs showing the states/actions/rl alg used in previous works. And one for graph representations showing which model is 1-WL among the trained one (GAT, GCNs, DeepGate, ...)
